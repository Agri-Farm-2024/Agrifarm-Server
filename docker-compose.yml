version: '3'
services:
  app:
    image: baohc1109/agri-farm-server
    container_name: agri-farm-server
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: always
