export enum RequestStatus {
  pending = 'pending',
  completed = 'completed',
  assigned = 'assigned',
  in_progress = 'in_progress',
  pending_approval = 'pending_approval',
  approved = 'approved',
}
