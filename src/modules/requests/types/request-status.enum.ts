export enum RequestStatus {
  pending = 'pending',
  assigned = 'assigned',
  in_progress = 'in_progress',
  pending_approval = 'pending_approval',
  rejected = 'rejected',
  completed = 'completed',
}
